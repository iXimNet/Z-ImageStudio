# Z-Image Studio environment configuration
# Copy to .env and adjust values as needed.

# Device selection: "cuda", "mps", or "cpu". Leave blank for auto-detect.
ZIMAGE_DEVICE=

# Torch dtype: "bf16", "fp16", "fp32". Leave blank for auto.
ZIMAGE_DTYPE=

# On Apple MPS + fp16, upcast UNet/transformer to fp32 to avoid black images.
# 1=true, 0=false. Default is 1 when MPS + fp16.
ZIMAGE_MPS_UNET_FP32=

# On Apple MPS, retry with fp32 UNet/transformer if NaNs/black image warnings appear.
# 1=true, 0=false. Default is 1.
ZIMAGE_MPS_NAN_FALLBACK=

# Enable CPU offload on CUDA: 1=true, 0=false.
ZIMAGE_CPU_OFFLOAD=0

# Keep multiple models loaded in memory: 1=true, 0=false.
# Set to 0 to release the previous model when switching.
ZIMAGE_KEEP_MODELS=0
